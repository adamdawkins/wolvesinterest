const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../interest';

describe('core.actions.interest', () => {
  it('should do something');
});
